API_KEY=your_secure_random_string_here
SHARED_DIR=./shared/
# CORS_ORIGINS defines which web origins are allowed to access the API.
# Separate multiple origins with commas. Do NOT use "*" in production.
# Example: CORS_ORIGINS=https://my-secure-frontend.com,http://localhost:5173
CORS_ORIGINS=http://localhost:5173
